import { Typography } from "@mui/material";
const Melodeal = () => {
  return (
    <Typography>
      A project I worked on which consisted of an iOS app aimed to connect musicians and venues to organise gigs. As main developer I developed the
      full iOS application, including UI/UX, and the backend server.
    </Typography>
  );
};

export default Melodeal;
